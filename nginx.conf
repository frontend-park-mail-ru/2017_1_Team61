
events {
    use epoll;
    multi_accept on;
}

http {

    server {
            include listen.conf;

            root /dist;
            location / {
                try_files /index.html /index.html;
            }
            location ~* \.(woff|oet|woff2|ttf|js|png|svg|ico|json)$ {
                try_files $uri $uri/ =404;
            }
        }
}
