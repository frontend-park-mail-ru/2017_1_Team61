import React from 'react';
import styles from './HeaderItem.css';

export const HeaderItem = ({children, align}) => (
    <div className={styles.container}>
        {}
    </div>
);

export default HeaderItem;
